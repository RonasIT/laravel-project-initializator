name: Run tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run-tests:
    uses: ronasit/github-actions/.github/workflows/tests.yml@main
    secrets: inherit
